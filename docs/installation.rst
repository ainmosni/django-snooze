============
Installation
============

At the command line::

    $ easy_install django-snooze

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-snooze
    $ pip install django-snooze